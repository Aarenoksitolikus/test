package ru.itis.oris.test.playground.model;

import com.slavikjunior.deorm.annotations.Column;
import com.slavikjunior.deorm.annotations.Id;
import com.slavikjunior.deorm.annotations.Table;
import com.slavikjunior.deorm.orm.Entity;
import java.sql.Types;
import java.util.Objects;

@Table(name = "comments")
public class Comment implements Entity {
    @Id(autogenerated = true)
    @Column(name = "id", nullable = false, type = Types.INTEGER)
    private int id;
    @Column(name = "post_id", nullable = false, type = Types.INTEGER)
    private int postId;
    @Column(name = "user_id", nullable = false, type = Types.INTEGER)
    private int userId;
    @Column(name = "content", nullable = false, type = Types.VARCHAR)
    private String content;

    public Comment(int id, int postId, int userId, String content) {
        this.id = id;
        this.postId = postId;
        this.userId = userId;
        this.content = content;
    }

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }

    public int getPostId() {
        return postId;
    }

    public void setPostId(int postId) {
        this.postId = postId;
    }

    public int getUserId() {
        return userId;
    }

    public void setUserId(int userId) {
        this.userId = userId;
    }

    public String getContent() {
        return content;
    }

    public void setContent(String content) {
        this.content = content;
    }

    @Override
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;
        Comment comment = (Comment) o;
        return id == comment.id && postId == comment.postId && userId == comment.userId && Objects.equals(content, comment.content);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id, postId, userId, content);
    }

    @Override
    public String toString() {
        return "Comment{" +
                "id=" + id +
                ", postId=" + postId +
                ", userId=" + userId +
                ", content='" + content + '\'' +
                '}';
    }
}
